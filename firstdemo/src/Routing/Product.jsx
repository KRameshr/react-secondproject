import React from "react";

const Product = () => {
  return (
    <div>
      const {val} = useParams(); return (
      <div className="container">
        <p>{val} :SD </p>
      </div>
      );
    </div>
  );
};

export default Product;
